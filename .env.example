# Required — app will not start without these
ANTHROPIC_API_KEY=
DATABASE_URL=
NEON_AUTH_BASE_URL=
NEON_AUTH_COOKIE_SECRET=
ADMIN_EMAIL=
BLOB_READ_WRITE_TOKEN=

# Optional — defaults applied in code if absent
NEXT_PUBLIC_SITE_URL=
NEXT_PUBLIC_APP_URL=
# Cloudflare Worker proxy URL for Reddit fetches (bypasses server IP blocks)
REDDIT_PROXY_URL=
